## Database schema scripts

This project uses Sequelize models, so tables can be created automatically via `sequelize.sync()`.

If you need **manual SQL** (for submission/evaluation), use:

- `mysql_schema.sql`: MySQL/InnoDB schema (includes `CREATE DATABASE smart_expernse`)

